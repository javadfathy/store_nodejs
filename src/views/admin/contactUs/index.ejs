<%- include('../include/header.ejs') %>
<div class="flex flex-row"> 
    <%- include('../include/sidebar.ejs') %>
    <section class="flex-1 pl-4 pt-4 container pr-4">
        <%- include('../../includes/breadcrumb.ejs', {breads: ['dashboard','contact us']}) %>
        <div class="relative overflow-x-auto shadow-md sm:rounded-lg">
            <table class="w-full text-sm text-left text-gray-500 dark:text-gray-400">
                <thead class="text-xs text-gray-700 uppercase bg-gray-50 dark:bg-gray-700 dark:text-gray-400">
                    <tr>
                        <th scope="col" class="p-4">
                            <div class="flex items-center">
                                <input id="checkbox-all-search" type="checkbox" class="w-4 h-4 text-blue-600 bg-gray-100 border-gray-300 rounded focus:ring-blue-500 dark:focus:ring-blue-600 dark:ring-offset-gray-800 dark:focus:ring-offset-gray-800 focus:ring-2 dark:bg-gray-700 dark:border-gray-600">
                                <label for="checkbox-all-search" class="sr-only">checkbox</label>
                            </div>
                        </th>
                        <th scope="col" class="px-6 py-3">
                            name
                        </th>
                        <th scope="col" class="px-6 py-3">
                            email
                        </th>
                        <th scope="col" class="px-6 py-3">
                            subject
                        </th>
                        <th scope="col" class="px-6 py-3">
                            phone
                        </th>
                        <th scope="col" class="px-6 py-3">
                            action
                        </th>
                    </tr>
                </thead>
                    <% if (contacts.length > 0) { %>
                <tbody>
                    
                        <% for (contact of contacts) { %>
                    <tr class="bg-white border-b dark:bg-gray-800 dark:border-gray-700 hover:bg-gray-50 dark:hover:bg-gray-600">
                        <td class="w-4 p-4">
                            <div class="flex items-center">
                                <input id="checkbox-table-search-1" type="checkbox" class="w-4 h-4 text-blue-600 bg-gray-100 border-gray-300 rounded focus:ring-blue-500 dark:focus:ring-blue-600 dark:ring-offset-gray-800 dark:focus:ring-offset-gray-800 focus:ring-2 dark:bg-gray-700 dark:border-gray-600">
                                <label for="checkbox-table-search-1" class="sr-only">checkbox</label>
                            </div>
                        </td>
                        <th scope="row" class="px-6 py-4 font-medium text-gray-900 whitespace-nowrap dark:text-white">
                            <%= contact.name %>
                        </th>
                        <td class="px-6 py-4">
                            <%= contact.email %>
                        </td>
                        <td class="px-6 py-4">
                            <%= contact.subject %>
                        </td>
                        <td class="px-6 py-4">
                            <%= contact.phone %>
                        </td>
                        <td class="px-6 py-4 space-x-3">
                            <a href="/admin/view-contact/<%= contact._id %>" class="inline-block font-medium text-blue-600 dark:text-blue-500 hover:underline">View</a>
                            <form action="/admin/delete-contact" class="inline-block" method="POST">
                                <button class="font-medium text-red-600 dark:text-red-500 hover:underline">Remove</button>
                                <input type="hidden" value="<%= contact._id %>" name="cotactId"/>
                            </form>
                        </td>
                    </tr>
                        <% } %>
                </tbody>
                <% } else { %>
                    <div class="p-4 mb-4 text-sm text-red-800 rounded-lg bg-red-50 dark:bg-gray-800 dark:text-red-400" role="alert">
                        <span class="font-medium">Not Found any Contact
                      </div>
                <% } %>
            </table>
        </div>
    </section>
</div>
<%- include('../include/footer.ejs') %>